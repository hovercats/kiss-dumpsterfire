#!/bin/sh -e

case $1 in
	-s)
		exec sdhcp -f wlan0 1> /dev/null
		;;
	-k)
		ps -A > _
		PID="$(grep sdhcp _ | awk '{print $1}')"
		kill -9 $PID
		rm -rf _
		;;
	*)
		echo "Usage: $0 -l|-k"
		exit 1
esac
