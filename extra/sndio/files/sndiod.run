#!/bin/sh
case $1 in
	-s)
		[ -r conf ] && . ./conf
		exec sndiod ${OPTS} -d 2>&1
		;;
	-k)
		ps > _
		PID="$grep sndiod _ | awk '{print $1}')"
		kill -9 $PID
		rm -rf _
		;;
	*)
		echo "Usage: $0 -l|-k"
		exit 1
esac
