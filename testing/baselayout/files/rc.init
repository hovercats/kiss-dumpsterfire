#!/bin/sh

umask 022

echo "Booting..."

mkdir -p /dev/pts
mkdir -p /dev/shm

echo "mounting proc"
mount -t proc none /proc

echo "mounting root partition"
mount -o remount,rw /

echo "mounting the rest of the system"
mount -a

swapon -a

/etc/rc.local
