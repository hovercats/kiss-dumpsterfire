#!/bin/sh

umask 022

echo "Booting..."

mount -t proc none /proc

mkdir -p /dev/pts
mkdir -p /dev/shm

echo "mounting root partition"
mount -o remount,rw /

echo "mounting the rest of the system"
mount -a

/etc/rc.local

svc -s 1> /dev/null
