#!/bin/sh -e

./configure \
    --prefix=/usr \
    --program-prefix=b \
    --enable-btyacc

make LDFLAGS="$LDFLAGS -static-pie" CFLAGS="$CFLAGS -fPIE"
make DESTDIR="$1" install

# Symlink to yacc
ln -s byacc "$1/usr/bin/yacc"
